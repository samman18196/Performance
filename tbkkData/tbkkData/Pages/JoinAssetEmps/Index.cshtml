@page
@model tbkkData.Pages.JoinAssetEmps.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JoinAssetEmp[0].Note)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JoinAssetEmp[0].Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.JoinAssetEmp) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.JoinAssetEmpID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.JoinAssetEmpID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.JoinAssetEmpID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
