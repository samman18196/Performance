@page
@model tbkkData.Pages.CarTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarType[0].CarTypeID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarType[0].NameCar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarType[0].Seat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CarType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CarTypeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameCar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Seat)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CarTypeID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CarTypeID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CarTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
