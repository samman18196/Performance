@page
@model tbkkData.Pages.AssetJoinNetworks.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AssetJoinNetwork[0].Note)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssetJoinNetwork[0].Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AssetJoinNetwork) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AssetJoinNetworkID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AssetJoinNetworkID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AssetJoinNetworkID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
